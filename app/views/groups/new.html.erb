<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <%= link_to groups_path, class: "text-soundcloud-orange hover:text-soundcloud-coral inline-flex items-center font-semibold transition-colors" do %>
      <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
      Back to Groups
    <% end %>
  </div>

  <div class="glass-card p-8">
    <h1 class="text-4xl font-bold mb-8 text-white">Create New Group</h1>

    <%= form_with model: @group, class: "space-y-6" do |f| %>
      <% if @group.errors.any? %>
        <div class="glass-medium rounded-xl p-6 border border-red-500/30 bg-red-500/10">
          <h3 class="font-semibold text-red-400 mb-3 text-lg">
            <%= pluralize(@group.errors.count, "error") %> prevented this group from being saved:
          </h3>
          <ul class="list-disc list-inside text-red-300 space-y-1">
            <% @group.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= f.label :name, "Group Name", class: "block text-sm font-bold mb-3 text-white" %>
        <%= f.text_field :name, class: "input-glass", placeholder: "My Awesome Music Group" %>
        <p class="text-xs text-gray-500 mt-2">Choose a memorable name for your music challenge group</p>
      </div>

      <div class="flex items-center justify-between pt-4">
        <%= f.submit "Create Group", class: "btn-primary cursor-pointer" %>
        <%= link_to "Cancel", groups_path, class: "text-gray-400 hover:text-white font-semibold transition-colors" %>
      </div>
    <% end %>
  </div>
</div>
